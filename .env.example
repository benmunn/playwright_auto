# Copy this file to .env and fill in your admin.reading-space.com credentials.
# .env is gitignored -- never commit real credentials.
RS_USERNAME=
RS_PASSWORD=

# Path to the workbook to write into. Relative paths resolve from the project root.
# Must already exist with its sheets and headers -- run make_template.py to create one.
RS_WORKBOOK=data/activities.xlsx
